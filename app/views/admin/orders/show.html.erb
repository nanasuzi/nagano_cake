<h1>管理者の注文履歴詳細ページ</h1>
<p>--注文履歴詳細--</p>

<p>購入者  <%= link_to "#{@order.name}", admin_customer_path(@order.customer_id) %></p>
<p>注文日  <%= @order.created_at.strftime('%Y/%m/%d') %></p>

<p>

配送先
  〒<%= @order.postal_code.to_s.insert(3, "-") %>
  <%= @order.address %>
    <div>
      <%= @order.name %>
    </div>
</p>

<p>
  注文ステータス
  <%= form_with model:@order, url:admin_order_path(@order.customer_id), method: :patch, local: true do |f| %>
  <%= f.select :order_status, Order.order_statuses.keys.to_a, {} %>
  <%= f.submit "更新" %>
  <% end %>
</p>

<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価（税込み）</th>
      <th>小計</th>
      <th>制作ステータス</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <% @order_detail.each do |order_detail| %>
      <td><%= order_detail.item.name %></td>
      <td><%= order_detail.item.price %></td>
      <td><%= order_detail.amount %></td>
      <td>
        <% sub_total = order_detail.item.price*order_detail.amount %>
        <%= sub_total %>
      </td>




      <% end %>
    </tr>
  </tbody>
</table>

