<h1>エンドユーザの注文確認画面</h1>
<p>--注文情報確認--</p>
<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価（税込み）</th>
      <th>数量</th>
      <th>小計</th>
    </tr>
  </thead>

  <tbody>
    <% sum = 0 %>
    <% @cart_items.each do |cart_item| %>
      <tr>

        <td><%= cart_item.item.name %></td>
        <td><%= cart_item.item.price %></td>
        <td><%= cart_item.amount %></td>

          <% sub_total = cart_item.item.price*cart_item.amount %>
        <td>
          <%= sub_total %>
        </td>
          <% sum += sub_total %>
      </tr>
    <% end %>

      <tr>
        <th>送料</th>
          <td><%= @order.shipping_cost %></td>
      </tr>

      <tr>
        <th>商品合計</th>
          <td><%= sum %></td>
      </tr>

      <tr>
        <th>請求金額</th>
          <td><%= @order.billing_amount = sum + @order.shipping_cost %></td>
      </tr>

  </tbody>
</table>

<p>--支払い方法--</p>
<%= @order.method_of_payment ? "クレジットカード" : "銀行振込" %>

<p>--お届け先--</p>
<%= @order.postal_code + @order.address + @order.name %>

<%= form_with model:@order, url: orders_path, method: :post, local: true do |f| %>
  <%= f.hidden_field :postal_code, :value => @order.postal_code %>
  <%= f.hidden_field :address, :value => @order.address %>
  <%= f.hidden_field :name, :value => @order.name %>
  <%= f.hidden_field :shipping_cost, :value => @order.shipping_cost %>
  <%= f.hidden_field :billing_amount, :value => @order.billing_amount %>
  <%= f.hidden_field :method_of_payment, :value => @order.method_of_payment %>
  <%= f.submit "注文を確定する" %>
<% end %>
